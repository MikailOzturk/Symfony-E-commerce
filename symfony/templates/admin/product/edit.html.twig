{% extends 'adminbase.html.twig' %}

{% block title %}ÜRÜN DETAYI{% endblock %}

{% block content %}

    <section class="content">
        <div class="container-fluid">
            <div class="block-header">
                <h1>
                    ÜRÜN DETAYI

                </h1>

            </div>
        </div>
        <div class="row clearfix">
            <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
                <div class="card">
                    <div class="header">

                        <small><a href="{{ path('admin_product_new') }}" class="btn btn-primary waves-effect" >Yeni Ürün Ekle</a> </small>

                        {{ include('admin/messages.html.twig') }}

                    </div>

                    {{ include('admin/product/_form.html.twig', {'button_label': 'Update'}) }}

                    <a href="{{ path('admin_product_index') }}"class="btn btn-primary waves-effect">back to list</a>

                    {{ include('admin/product/_delete_form.html.twig') }}
                </div>
            </div>
        </div>



    </section>
{% endblock %}
